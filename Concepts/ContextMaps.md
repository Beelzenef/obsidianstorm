Se realizan diagramas de influencia entre contextos, haciendo upstream (U) y downstream (D), el contexto hace upstream o influencia hacia el contexto en downstream.

Se generan varios tipos de relaciones:

- Consumer/Supplier
- Conformist
- Partners